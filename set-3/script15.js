let isRaining = true;
let isSnowing = false;
let windspeed = 15;
let cloudy = true;

if (isRaining === false && isSnowing === true && windspeed === 15 && cloudy === true) {
	console.log("wheather is good");
} else {
	console.log("wheather is bad");
}